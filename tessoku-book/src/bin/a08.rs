use proconio::input;

fn main() {
    input! {
        h: usize,
        w: usize,
        x: [[usize; w]; h],
        q: usize,
        abcd: [[usize; 4]; q]
    };

    let mut acc = vec![vec![0; w]; h];

    // 横方向のacc

    // 縦方向のacc

    // 総和を計算
}
