use proconio::input;

fn main() {
    input! {
        n: usize,
        s: String,
    };

    let mut groups: Vec<String> = Vec::new();
    let mut current_char: Option<char> = None;

    for i in 0..n {}
}
